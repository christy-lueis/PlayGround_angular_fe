import { AdminRoleDirective } from './admin-role.directive';

describe('AdminRoleDirective', () => {
  it('should create an instance', () => {
    const directive = new AdminRoleDirective();
    expect(directive).toBeTruthy();
  });
});
